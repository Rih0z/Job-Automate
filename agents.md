# エージェント分離アーキテクチャ

> 実装とレビューを**別エージェント（別セッション）**で実行し、客観的な品質保証を行うための設計。

---

## なぜエージェントを分離するか

同一セッションでの実装とレビューには以下の問題がある：

1. **コンテキスト汚染**: 実装時の思考過程・設計判断を知っているため、「なぜそうしたか」を好意的に解釈してしまう
2. **確証バイアス**: 自分が書いた（または書く過程を見た）コードの問題を見つけにくい
3. **甘い採点**: 実装の苦労を知っているため、無意識にスコアを上げてしまう

**解決策**: レビューエージェントには **コードと差分のみ** を渡し、実装の意図・設計判断・会話履歴は渡さない。

---

## エージェント一覧

| エージェント | 役割 | 起動方法 | コンテキスト分離 |
|-------------|------|----------|----------------|
| **review-changes** | 変更差分のテスト・実装レビュー | `/review-changes` → Agent ツールで自動起動 | 実装コンテキストを遮断。コード差分のみ渡す |

---

## エージェントへの入力設計（何を渡す/渡さないか・汎用原則）

**このリポジトリで新しいレビューエージェントを追加する場合、以下2原則は特定のエージェント固有ではなく全エージェント共通の設計原則として適用する。**

### 原則1: 実装コンテキストの遮断

```
実装セッション（あなた）           レビューエージェント（別プロセス）
┌─────────────────────┐          ┌──────────────────────────┐
│ 実装の思考過程       │    ✗     │                          │
│ 設計判断の理由       │ ───×───→ │  受け取るもの:            │
│ ユーザーとの会話     │          │  - 評価対象（diff/成果物）│
│ 試行錯誤の履歴       │          │  - 評価基準ファイル       │
│                     │    ✓     │  - （必要なら）テスト設定 │
│ 評価対象（diff等）   │ ────────→│    等の事実情報           │
│ 評価基準ファイル     │          │                          │
└─────────────────────┘          └──────────────────────────┘
```

**渡してよいもの（事実情報のみ）:**
- 評価対象そのもの（`git diff` / `git log` の出力、変更ファイル一覧・内容、成果物ファイル等）
- 評価基準ファイル（各 `workflows/<workflow-name>/review-*.md` 等）
- テスト設定・CI設定ファイル（該当する場合）

**渡してはいけないもの（実装コンテキスト）:**
- 実装の意図・目的の説明
- 設計判断の理由
- 「ここは後で直す予定」等の予告
- ユーザーとの会話履歴
- 実装セッションでの試行錯誤

### 原則2: 並列起動時のアンカリング防止

複数のレビューエージェントを並列起動する場合、原則1（実装コンテキストの遮断）とは別軸で、**他のレビューエージェントの判定結果も互いに渡さない**。あるレビュアーの指摘を先に見せてしまうと、後続のレビュアーの独立判断がその指摘に引きずられる（アンカリング）。各レビュアーは同一の対象・評価基準のみを受け取り、他レビュアーの存在を知らない状態で判定してから、結果を突き合わせる。

### 原則3: コーディネーターの中継はメタデータを保持する

複数の調査・取得系サブエージェント（web検索・文書分析等）の結果を、コーディネーターが**別の統合（synthesis）エージェント**へ引き渡す構成を設計する場合、各結果の出典（URL・ファイルパス・文書名等）を本文内容と分離した構造化データとして保持したまま渡す。コーディネーターが「要点だけ」を平文で抜き出して渡すと、統合エージェントには出典を引用する材料がそもそも無くなり、根拠のない主張を含む成果物が生成される。取得系サブエージェント自体が正しく出典付きの結果を返していても、この中継部分で構造が失われれば無意味になる。対策は統合エージェント側の指示強化ではなく、コーディネーターが中継時に `{content, source_url, source_path}` のような形でメタデータを本文と紐付けたまま渡すこと。

（本リポジトリの現行スキルは「コーディネーターが事実を1回集めて単一エージェントへ渡す」または「1ファイル/1サンプル=1エージェントが完結した判定を返し、コーディネーターは結果を並べるだけ」のいずれかで、複数調査結果を単一の統合エージェントへ中継する構成は現状無い。新規に調査集約型のワークフローを設計する時のための予防原則。）

以下の「review-changes エージェント」は原則1・2を1つのエージェントに適用した具体例。新規エージェントを設計する際は、この具体例をテンプレートとして自分のエージェント用に書き換える。

---

## review-changes エージェント

### 概要
直近の変更に対して、実装の正確性とテストの網羅性を客観的に100点満点で評価する。

### 分離ルール（上記2原則の review-changes への適用）

- 渡すもの: `git diff`（コード差分）・変更ファイル一覧・テスト設定ファイル・`workflows/software-development/review-changes.md`（評価基準）
- 渡さないもの: 上記「渡してはいけないもの」に同じ

### 起動手順（自動）

`/review-changes` コマンドを実行すると、以下が自動で行われる：

1. **情報収集**（実装セッション側）
   - `git diff HEAD~1` / `git diff --cached` / `git diff` を取得
   - 変更ファイル一覧を取得
   - テスト設定ファイルを検出
   - `workflows/software-development/review-changes.md` の内容を取得

2. **レビューエージェント起動**（Agent ツール）
   - 収集した情報のみをプロンプトに含める
   - `workflows/software-development/review-changes.md` の評価基準をプロンプトに含める
   - Agent ツールの `subagent_type: "general-purpose"` で起動

3. **結果の返却**
   - レビューエージェントの評価結果を実装セッションに返す
   - ユーザーに構造化されたレビューレポートを表示する

### 手動での分離実行

自動起動ではなく手動で分離したい場合：

```
ターミナル1（実装セッション）:
  → コードを実装する

ターミナル2（レビューセッション — 別の Claude Code を起動）:
  → workflows/software-development/review-changes.md を読み込ませる
  → 「このプロジェクトの直近の変更をレビューしてください」と依頼
  → レビューエージェントは git diff を自分で取得してレビューする
```

---

## 新しいエージェントを追加する方法

1. このファイルの「エージェント一覧」テーブルに追記する
2. `.claude/skills/[skill-name]/SKILL.md` を作成する（オーケストレーター。YAML frontmatter 必須）
3. 評価基準を `workflows/<ワークフロー名>/review-[対象].md` に作成する
4. 分離ルール（何を渡すか・何を渡さないか）を明記する
5. `CLAUDE.md` のスキルテーブルに追記する

### エージェント設計の原則

| 原則 | 説明 |
|------|------|
| **コンテキスト最小化** | エージェントに渡す情報は必要最小限の事実のみ |
| **証拠主義** | 推測でスコアを付けない。コードから確認できる事実のみで評価 |
| **非破壊** | レビューエージェントはコードを編集しない。レビューのみ |
| **再現性（ベストエフォート）** | 同じ入力に対して近い評価が得られること。LLMベースのため完全な再現性は保証されない |
| **採点の厳格性** | 証拠不十分時は各レンジの下限で採点。好意的解釈をしない |
| **ツールスコープの限定** | 1エージェントの `allowed-tools` は4〜5個・単一ロールを目安にする。全く異なる専門領域（例: コード分析・セキュリティスキャン・デプロイ検証）を1エージェントに詰め込むと選択精度が落ちる。複数領域を横断する必要がある場合は、1エージェントに多ツールを持たせるのではなく、対象単位（ファイル1件・工程1件等）ごとにロール別のエージェントを分けて起動する（`harness-compliance-audit` の変更ファイル1件=1エージェント起動が具体例） |

---

## 長時間・並列 subagent 運用のクラッシュ耐性（構造化 state manifest）

一晩がかりの探索など、長時間かつ複数の subagent を並列起動するタスクでは、コーディネーター（起動元セッション）自身が再起動・クラッシュすると、再実行時にどこまで終わっていたかを再現できず全 subagent をゼロからやり直しがちになる。

**避けるべき対策**:
- ツール呼び出しのリトライ回数を増やす（ツール単発の失敗には効くが、プロセス全体の再起動には無力）
- コーディネーターのセッションを `--resume` で復元する（コーディネーター自身の会話は戻るが、各 subagent が既にどこまで進んでいたかは分からない）
- 作業を1つの逐次 subagent に直列化する（並列化のメリットを失う代償が大きい）

**正しい対策**: 各 subagent に、完了を待たず**進捗を構造化データとして既知の場所へ都度書き出させる**。

- 各 subagent は担当領域の発見内容を、節目ごとに `<作業ディレクトリ>/<subagent 名>.json`（manifest）へ永続化する
- コーディネーターは再起動後まず manifest 群を読み、どの subagent が完了済みかを機械的に判定する
- 未完了の subagent だけを、既存 manifest の内容（部分的な発見済み事項）を種として再起動する。完了済みの subagent は再実行しない

Claude Code の `Workflow` ツール自体が持つ `resumeFromRunId`（完了済み `agent()` 呼び出しの結果キャッシュ）はこの発想の一部を担うが、**同一の (prompt, opts) 呼び出し単位でしか再利用できない**。1つの subagent が長時間かけて内部的に探索を進めている途中でクラッシュした場合、その subagent 自身が manifest を書き出していなければ、その呼び出し自体は最初からやり直しになる。両者は併用する。

新規に長時間・並列 subagent を伴うエージェント/ワークフローを設計する際は、各 subagent の prompt に manifest 書き出し先の絶対パスを明記すること。

---

## 制約と限界

| 制約 | 説明 | 緩和策 |
|------|------|--------|
| **コンテキスト遮断はベストエフォート** | Agent ツールは別プロセスだが、完全な情報遮断を技術的に保証するものではない | 手動分離（別ターミナル）を併用することで確実性を上げる |
| **静的分析のみ** | テストの実行結果（パス/フェイル）は確認できない | CI結果やテスト実行ログがあれば併せて渡す |
| **LLMの非決定性** | 同じ入力でも毎回微妙に異なる評価になりうる | 複数回実行して傾向を確認する。スコアの±5点は誤差範囲と捉える |
| **プロンプト依存** | レビュー品質はプロンプトの指示に依存する | 定期的にレビュー結果を人間が検証し、プロンプトを改善する |
