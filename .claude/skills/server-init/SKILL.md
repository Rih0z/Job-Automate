---
name: server-init
description: |
  レンタルサーバー環境と手元の開発環境をSSHで接続できるように初期設定する。ブログ/サイトの
  アドレスとレンタルサーバー管理会社名を受け取り、まずSSH接続手順を案内し、SSH設定済みの場合は
  ローカル環境で編集してから問題なければサーバーへアップロードする管理体制の環境構築を支援する。
  Trigger phrases: 'サーバー SSH接続 設定', 'レンタルサーバー ssh 設定方法', 'ブログをローカルで
  管理したい', 'server init', 'SSH環境構築', 'サーバー初期設定'.
metadata:
  provenance: domain-prompt
---

# サーバー初期設定 — SSH接続環境構築

## 手順

1. Claude Web版を利用して、自分の開発環境と借りているレンタルサーバーの環境をSSHで接続できる
   環境を構築する。

2. 以下のプロンプトパターンでユーザーから情報を受け取り、案内する。

### パターン1: SSH接続設定がまだの場合

```
[ブログアドレス]を[サーバー管理会社]レンタルサーバーで運用している。ssh接続がしたい。設定方法を教えて。
```

上記の依頼を受けたら、対象のレンタルサーバー管理会社が提供するSSH接続手順 (SSH鍵生成・公開鍵
登録・接続確認) を具体的に案内する。

### パターン2: SSH接続設定済みの場合

```
[ブログ名]を[サーバー管理会社]レンタルサーバーで運用している。sshの設定はしている。このブログに
ついて、claudeで管理できるようにしたい。ローカル環境で編集して、問題がなければサーバーに
アップロードする形である。このようなことはできる？できるなら環境構築を手伝って。
```

上記の依頼を受けたら、ローカル編集 → 検証 → サーバーへアップロードのワークフローが実現可能で
あることを説明し、必要なファイル同期・バックアップ体制の環境構築を支援する。継続的な自動化
ワークフロー構築 (バックアップ・記事更新・ファクトチェック等) は `.claude/skills/server-automation/SKILL.md`
に引き継ぐ。

## 関連 skill
- 継続運用・自動化ワークフロー: `.claude/skills/server-automation/SKILL.md`
