# /review-implementation — 実装品質 総合レビュー

> プロジェクト全体の実装品質を5軸（テスト・正確性・マネタイズ・ペルソナ・UX）で100点満点評価する。
> 詳細な評価基準は `workflows/software-development/review-implementation.md` を参照。

> **このコマンドは別エージェントを起動してレビューを行う。現在のセッションでは直接レビューしない。**
> 実装コンテキスト（思考過程・設計判断・会話履歴）をレビューエージェントに渡さないことで客観性を確保する。

---

## 実行手順

### ステップ1: 情報収集（この実装セッションで行う）

以下の事実情報のみを収集する。実装の意図・目的・設計判断の説明は**含めない**。

```
収集対象:
1. ペルソナ定義: workflows/software-development/design/persona.md
   （なければ「未検出」と記録するのみ。推定ユーザーの創作・注記はここでは行わない）
2. 収益モデル: README 等に記載の内容（なければ「未検出」と記録）
3. テスト戦略: jest.config.* / vitest.config.* / pytest.ini 等のテスト設定ファイルの検出と内容
4. デザインガイドライン: workflows/software-development/design/design-guidelines.md
   （なければ「未検出」と記録）
5. コーディング規約: workflows/software-development/rules/coding-principles.md
6. プロジェクトの実装成果物一覧（対象ディレクトリのファイル構成・主要ソースファイル）
7. workflows/software-development/review-implementation.md の内容
   （評価基準の詳細チェックリスト — プロジェクト内にあれば読み込む）
```

**収集の原則:**
- 収集するのは事実情報（設定ファイルの中身・ファイル一覧・ドキュメントの記載内容）のみ
- 実装の意図・設計判断の解説・会話履歴の要約はここに含めない
- 各項目が未検出の場合は「未検出」とだけ記録し、代替の推定・仮定はステップ1では行わない
  （代替方針の適用はステップ2のレビューエージェント側の判断に委ねる）

### ステップ2: レビューエージェント起動

**Agent ツール**を使い、以下の設定で別エージェントを起動する:

```
Agent ツールの設定:
- subagent_type: "general-purpose"
- description: "Review implementation quality objectively"
- prompt: 以下のテンプレートに収集した情報を埋め込む
```

**プロンプトテンプレート（レビューエージェントに渡す内容）:**

```
あなたはシニアアーキテクト兼品質保証エキスパートです。
実装者とは別の客観的な立場から、プロジェクトの実装成果物を5軸（テスト・正確性・マネタイズ・ペルソナ・UX）で
100点満点評価してください。

## 行動原則
- 証拠主義: コードの事実のみに基づいて評価する。推測でスコアを付けない
- 各スコアにはファイルパスと行番号の引用を最低1つ以上含めること
- 非破壊: レビューのみ行い、コードの編集・実行は行わない
- 証拠不十分時は各レンジの下限で採点する

## 前提情報（ステップ1で収集した事実情報）
- ペルソナ定義: [ここにステップ1で収集したペルソナ定義の内容、または「未検出」を埋め込む。
  未検出の場合は推定ユーザーを仮定し、その旨をレポートに注記すること]
- 収益モデル: [ここにステップ1で収集した収益モデルの内容、または「未検出」を埋め込む。
  未検出の場合はマネタイズ軸をN/Aとし80点満点に換算すること]
- テスト戦略: [ここにステップ1で検出したテスト設定を埋め込む]
- デザインガイドライン: [ここにステップ1で収集したデザインガイドラインの内容、または「未検出」を埋め込む。
  未検出の場合は一般的UXベストプラクティスで評価すること]
- コーディング規約: [ここにステップ1で収集したコーディング規約の内容を埋め込む]
- プロジェクトの実装成果物一覧: [ここにステップ1で収集したファイル構成・主要ソースファイル一覧を埋め込む]

## 評価軸（5項目・各20点 = 100点満点）

[評価軸の詳細は本ファイル「評価軸」セクションをそのまま埋め込む]

## 詳細な評価基準

[ここに workflows/software-development/review-implementation.md の内容を埋め込む。
 プロジェクト内にある場合は詳細なチェックリストとして使用すること]

## 採点・判定

[本ファイル「採点・判定」セクションの表をそのまま埋め込む]

## 出力フォーマット

[本ファイル「出力フォーマット」セクションのフォーマットをそのまま埋め込む]
```

### ステップ3: 結果の報告

レビューエージェントから返却された結果を、そのままユーザーに表示する。
要約や解釈を加えない（レビューの客観性を維持するため）。

---

## 評価軸（5項目・各20点 = 100点満点）

### 1. テスト実装品質（20点）
- テストファースト証跡（失敗→成功の流れ）
- カバレッジ（正常系・異常系・境界値）
- テスト品質（フレーキーでないか、ハードコードがないか）
- 失敗シグナルの複数明示（戻り値だけでなく例外種別・エラーメッセージ・ログ・副作用の状態等）
- テストの保存場所・実行経路が一意に定義されているか

### 2. 処理の正確性（20点）
- ビジネスロジックの正確性
- エラーハンドリング
- セキュリティ・データ整合性

### 3. マネタイズ設計整合性（20点）
- 収益モデルとの整合
- 収益に影響するUX
- 分析・計測の実装

### 4. ペルソナ・クライアント適合実装（20点）
- ペルソナ定義との整合
- ペインポイント解消
- 文言・ラベルの適切さ

### 5. UX品質（20点）
- インタラクション品質
- ビジュアル・レイアウト品質
- アクセシビリティ・パフォーマンス

---

## 詳細な評価基準

プロジェクト内に `workflows/software-development/review-implementation.md` がある場合は、その内容を読み込んで詳細なチェックリストとして使用すること。

---

## 採点・判定

| 評価 | 点数 | 判定 |
|------|------|------|
| S | 90〜100 | リリース可 |
| A | 80〜89  | 軽微な修正後にリリース可 |
| B | 70〜79  | 重点項目の修正が必要。再レビュー |
| C | 60〜69  | 大幅な修正が必要 |
| D | 59以下  | 根本的な再設計が必要 |

---

## 出力フォーマット

```markdown
# 実装品質 レビュー結果

## 基本情報
- プロジェクト名:
- レビュー日:
- ペルソナ定義の参照先:
- 収益モデル:
- テスト戦略:

## 総合評価
- **総合点: XX / 100点**
- **判定: X (S/A/B/C/D)**

## 観点別採点

| # | 観点 | 配点 | 得点 | 主なコメント |
|---|------|------|------|-------------|
| 1 | テスト実装品質 | 20 | | |
| 2 | 処理の正確性 | 20 | | |
| 3 | マネタイズ設計整合性 | 20 | | |
| 4 | ペルソナ・クライアント適合実装 | 20 | | |
| 5 | UX品質 | 20 | | |

## 良かった点（Keep）

## 改善提案（Problem / Try）

## クリティカルイシュー

| # | 問題 | 影響 | 優先度 | 対処方法 |
|---|------|------|--------|----------|
| | | | 高/中/低 | |

## 次のアクション

1. 【必須】
2. 【推奨】
3. 【任意】
```

---

## 注意事項

- **絶対に実装セッション内で直接レビューしない**。必ず Agent ツールで別エージェントを起動すること
- レビューエージェントに渡す情報は事実（設定ファイルの中身・ファイル一覧・ドキュメントの記載内容）のみ。実装の意図・設計判断・会話履歴は渡さない
- 対象範囲が小さい場合でも分離実行する。規模で判断しない
